{
  "name": "componentes.gov.br",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "ng": "ng",
    "watch-scss": "node-sass --watch **/* -o **/*",
    "build-scss": "node-sass -r componentes -o componentes",
    "test": "echo \"Error: no test specified\" && exit 1",
    "validate:html": "node_modules/.bin/html-validate componentes/**/*.html",
    "validate:sass": "node_modules/.bin/sass-lint -c .sass-lint.yml -v -q"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "html-validate": "^1.1.0",
    "node-sass": "^4.12.0",
    "nodemon": "^1.19.1",
    "sass-lint": "^1.13.1"
  }
}
