extends ../../layouts/component

append vars
  - var title = "DatePicker";

append scripts
  script(src="../js/components/datepicker.min.js")
  script(src="../js/components/datepicker.js")

append content
  .row
    .col-sm
      .mb-3
        +input("Normal")(id="default" class="datepicker" placeholder="dd/mm/aaaa")
        p.help Insira a data no formato "dd/mm/aaaa", apenas números.  
        +feedback("Data válida")(class="is-valid")
        +feedback("Data inválida")
    .col-sm
  .row
    .col-sm
      .mb-3
        +input("Desativado")(id="disabled" class="datepicker" placeholder="01/01/2020" disabled)
  
    .col-sm
      .mb-3
        +input("Focus")(id="focus" class="datepicker is-focus" placeholder="dd/mm/aaaa")
  .row
    .col-sm
      .mb-3
        +input("Válido")(id="valid" class="datepicker is-valid" placeholder="dd/mm/aaaa")
        +feedback("Data válida")(class="is-valid")
    .col-sm
      .mb-3
        +input("Inválido")(id="invalid" class="datepicker is-invalid" placeholder="dd/mm/aaaa")
        +feedback("Data inválida")
  .row
    .col-sm
      .mb-3
        +input("Data Inicial")(id="date-start" class="datepicker" type="text" placeholder="dd/mm/aaaa")
        p.help Insira a data inicial do período no formato "dd/mm/aaaa".  
        +feedback("Data válida")(class="is-valid")
        +feedback("Data inválida")
    .col-sm
      .mb-3
        +input("Data Final")(id="date-end" class="datepicker" type="text" placeholder="dd/mm/aaaa")
        p.help Insira a data final do período no formato "dd/mm/aaaa".
        +feedback("Data válida")(class="is-valid")
        +feedback("Data inválida")
