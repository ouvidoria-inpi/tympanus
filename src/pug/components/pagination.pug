mixin pagination
  - var pages = 10;
  - var page = 3;
  nav
    ul.br-pagination(class=attributes.class)
      li.is-first.is-disabled
        a(href="")
          i.fas.fa-chevron-left
          span.sr-only primeiro
      li.is-active
        a(href="") 1
      li: a(href="") 2
      li.d-sm-none
        label.sr-only(for=attributes.id)
        select(id=attributes.id)
          while page < pages
            option=page++
      - var page = 3;
      while page < pages
        li.d-none.d-sm-inline
          a(href="")=page++
      li: a(href="")=pages
      li.is-last
        a(href="")
          i.fas.fa-chevron-right
          span.sr-only último
