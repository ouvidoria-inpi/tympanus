mixin checkbox(label="Label")
  - let className = attributes.class
  - let componentClass = prefix + "checkbox"
  - className = className ? componentClass + " " + className : componentClass
  if attributes.invalid
    div(class=className, name=attributes.name, invalid)
      input(
        id=attributes.id
        type="checkbox"
        checked=attributes.checked
        disabled=attributes.disabled)
      label(for=attributes.id)=label
      if block
        block
  else if attributes.valid
    div(class=className, name=attributes.name, valid)
      input(
        id=attributes.id
        type="checkbox"
        checked=attributes.checked
        disabled=attributes.disabled)
      label(for=attributes.id)=label
      if block
        block
  else
    div(class=className, name=attributes.name)
      input(
        id=attributes.id
        type="checkbox"
        checked=attributes.checked
        disabled=attributes.disabled)
      label(for=attributes.id)=label
      if block
        block

