mixin switch(label="Label")
  - let className = attributes.class
  .br-switch(class=className)
    input( id=attributes.id type="checkbox" aria-label=attributes.ariaLabel checked=attributes.checked disabled=attributes.disabled)
    label(for=attributes.id)=label
    if block
      block
mixin switch-default
  +switch("Label")#switch-default(checked)

mixin switch-disabled
  +switch("Label")#switch-disabled(checked disabled)

mixin switch-small
  +switch("Pequeno").small.mr-5#switch-small(checked)
mixin switch-medium
  +switch("Médio").medium#switch-medium(checked)

mixin switch-large
  +switch("Grande").large.mr-5#switch-large(checked)

mixin switch-label-onoff
  +switch("Label").medium#switch-label-01(checked)
    .switch-data(data-enabled="Ligado" data-disabled="Desligado")

mixin switch-label-terms
  +switch("Label").medium#switch-label-02(checked)
    .switch-data(data-enabled="Concordo" data-disabled="Discordo")

mixin switch-left
  +switch("Label").mr-5#switch-left(checked)

mixin switch-right
  +switch("Label").right.mr-5#switch-right(checked)

mixin switch-top
  +switch("Label").top.mr-5#switch-top(checked)

mixin switch-icon
  +switch("Label").icon#switch-icon(checked)
